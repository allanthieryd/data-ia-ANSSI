"""Package du pipeline ANSSI : extraction, enrichissement, consolidation.

Les modules s'executent depuis la racine du projet, soit via ``main.py``,
soit directement avec ``python -m scripts.<module>`` (les imports sont
relatifs au package).
"""
